import React from "react";
import ReactDOM from "react-dom";

const Application = function() {
    return (
        <h1>KIKOU LES FURFAGS</h1>
    );
}

ReactDOM.render(<Application />, document.getElementsByTagName("main")[0]);
